# Core runtime dependencies for the PDF translator
# You can loosen pins if needed; these ranges are chosen for compatibility
langchain>=0.2.0,<0.3.0
langchain-openai>=0.1.0,<0.2.0
langchain-community>=0.2.0,<0.3.0
pypdf>=4,<5
PyYAML>=6.0
